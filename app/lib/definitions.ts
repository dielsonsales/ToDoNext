export type Task = {
  id: string;
  icon: string;
  title: string;
  // note: string;
  // remindData: Date;
  // dueDate: Date;
  // done: boolean;
};

export type CustomTaskList = {
  id: string;
  icon: string;
  title: string;
};
